<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.godlight">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.account.LoginActivity" >

        </activity>
        <activity android:name=".ui.account.RegisterActivity">


        </activity>

        <activity android:name=".ui.account.CeShi">

        </activity>
        <activity android:name=".ui.account.MineActivity">

        </activity>
        <activity android:name=".ui.account.SettingsActivity"></activity>
        <activity android:name=".ui.account.ChangedActivity"></activity>
        <activity android:name=".ui.account.AddressActivity"></activity>
        <activity android:name=".ui.account.RegisterSecondeActivity"></activity>
        <activity android:name=".ui.account.AddJiaActivity"></activity>
    </application>

</manifest>